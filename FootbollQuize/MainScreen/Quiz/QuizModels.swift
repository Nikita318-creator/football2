import Foundation

struct QuizProgress {
    let current: Int
    let total: Int
}

struct QuizCategory {
    let image: String
    let title: String
    let subtitle: String
    let solvedQuestions: Int
    let totalQuestions: Int
}

